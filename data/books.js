/**
 * Array that holds the list of books.
 *
 * This array is intended to store book objects with various properties
 * such as id, name, author, etc. Initially, it is an empty array but
 * can be populated with book data dynamically.
 *
 * @type {Array<Object>}
 */

const books = [];

export default books;
